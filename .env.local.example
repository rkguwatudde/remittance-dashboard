# API gateway (microservices). Browser should use the same-origin proxy.
# NEXT_PUBLIC_REMITTANCE_API_URL=/api/remittance-backend
# REMITTANCE_API_UPSTREAM=http://localhost:9000
#
# Staging gateway: https://staging-api.borabond.com
#we are using staging-api.borabond.com for staging and api.borabond.com for production
# Production gateway: https://api.borabond.com
